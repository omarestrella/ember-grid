<div class="hex-board">
  <div class="grid zoom-3">
    {{#each this.grid as |row|}}
      <div class="row">
        {{#each row as |hex|}}
          <HexBoard::HexTile @hex={{hex}} @game={{@game}}></HexBoard::HexTile>
        {{/each}}
      </div>
    {{/each}}
  </div>
</div>
